{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h1>Interessentgrundmapping</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interessentgrundmapping.id }}</td>
            </tr>
            <tr>
                <th>Annulleret</th>
                <td>{{ interessentgrundmapping.annulleret }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('interessentgrundmapping_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('interessentgrundmapping_edit', { 'id': interessentgrundmapping.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
