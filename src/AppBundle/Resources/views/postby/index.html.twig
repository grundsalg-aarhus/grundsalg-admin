  {% extends 'AppBundle::base.html.twig' %}


  {% block body -%}

  <div class="row heading">
    <div class="col-md-9">
        <h1>{{ 'postby.labels.plural' | trans }}</h1>
    </div>
    <div class="col-md-3 text-right">
              <a class="btn btn-default" href="{{ path('postby_new') }}">
          {{ icon('plus') }} {{ 'postby.actions.add' | trans }}
        </a>
          </div>

  </div>


  <table class="table table-condensed table-header-rotated">
    <thead>
    <tr>
        {{ helper.header('appbundle.postby.postalcode') }}
        {{ helper.header('appbundle.postby.city') }}
        {{ helper.header('appbundle.postby.createdby') }}
        {{ helper.header('appbundle.postby.createddate') }}
        {{ helper.header('appbundle.postby.modifiedby') }}
        {{ helper.header('appbundle.postby.modifieddate') }}
      {{ helper.header() }}
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
    <tr>
                  <td>{{ entity.postalcode }}</td>
        
                  <td>{{ entity.city }}</td>
        
                  <td>{{ entity.createdby }}</td>
        
                  <td>{{ entity.createddate | date("d/m Y") }}</td>
        
                  <td>{{ entity.modifiedby }}</td>
        
                  <td>{{ entity.modifieddate | date("d/m Y") }}</td>
        
      <td class="text-right">    <a class="btn btn-default btn-xs" href="{{ path('postby_edit', { 'id': entity.id }) }}">{{ 'common.edit' | trans }}</a>

      </td>

    </tr>
    {% endfor %}
    </tbody>
  </table>

  {% endblock %}
