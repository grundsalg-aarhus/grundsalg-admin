{% block extends %}
{{ "{% extends 'AppBundle::base.html.twig' %}" }}
{% endblock extends %}

{% set class = entity | split('\\') | last | lower %}

{% block body %}
{{ "{% block body %}" }}
    <div class="row heading">
        <div class="col-md-9">
            <h1>{{ "{{ '" }}{{ class }}{{ ".actions.edit' | trans }}" }}</h1>
        </div>
        <div class="col-md-3 text-right">
            {%- if ('delete' in actions) %}

                {{ '{{ form_start(delete_form) }}' }}
                <input type="submit" class="btn btn-danger" value="{{ "{{ 'common.delete' | trans }}" }}">
                {{ '{{ form_end(delete_form) }}' }}

            {%- endif %}
        </div>
    </div>

    {{ '{% form_theme edit_form "AppBundle::Form/twocol-form.html.twig"  %}' }}
    {{ '{{ form_start(edit_form) }}' }}
    {%- for field in fields %}
        {%- if field | first != 'id'  %}
        {{ '
    {{ form_row(edit_form.' }}{{ field | first }}{{ ')}}' }}
        {%- endif  %}
    {%- endfor %}

    {{ '{{ form_end(edit_form) }}' }}


    <div class="form-actions">
        <input type="submit" class="btn btn-primary" value="{{ "{{ 'common.edit' | trans }}" }}" />
    </div>


{{ "{% endblock %}" }}
{% endblock body %}
