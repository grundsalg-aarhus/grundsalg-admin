{% block extends %}
{{ "{% extends 'AppBundle::base.html.twig' %}" }}
{% endblock extends %}

{% set class = entity | split('\\') | last | lower %}

{% block body %}
{{ "{% block body %}" }}
    <div class="row heading">
        <div class="col-md-9">
            <h1>{{ "{{ '" }}{{ class }}{{ ".actions.new' | trans }}" }}</h1>
        </div>
        <div class="col-md-3 text-right"></div>
    </div>

    {{ '{% form_theme form "AppBundle::Form/twocol-form.html.twig"  %}' }}
    {{ '{{ form_start(form) }}' }}
        {{ '{{ form_widget(form) }}' }}
        <input type="submit" value="Create" />
    {{ '{{ form_end(form) }}' }}

{{ "{% endblock %}" }}
{% endblock body %}
